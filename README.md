# Antarctic Research Station Telemetry Dashboard

Live telemetry pipeline connecting a Python data simulation engine to a real-time React web dashboard.

---

## Architecture Overview

```
┌────────────────────────────────────────┐
│   data_simulator.py (Python 3.13)      │
│   ├── Background Daemon Thread         │
│   │   ├── generate_data()              │
│   │   ├── prints JSON to stdout        │
│   │   └── updates thread-safe cache    │
│   │                                    │
│   └── Flask REST API (Port 5000)       │
│       ├── GET /api/health              │
│       └── GET /api/data  ◄─────────────┼───┐
└────────────────────────────────────────┘   │
                                             │ HTTP GET (fetch every 2s)
                                             │ CORS enabled
┌────────────────────────────────────────┐   │
│   React Frontend (Vite • Port 3000)    │   │
│   ├── src/App.jsx ─────────────────────┴───┘
│   ├── src/components/
│   │   ├── Sidebar.jsx (Responsive Navigation)
│   │   ├── Topbar.jsx (Connection Badge & UTC Clock)
│   │   ├── StationStatus.jsx (Runtime & Health)
│   │   ├── AnomalyAlert.jsx (Dynamic Alerts)
│   │   ├── StatCard.jsx (Reusable Metric Cards)
│   │   ├── EnergyCard.jsx, GeneratorCard.jsx, ...
│   │   ├── TemperatureCard.jsx, VibrationCard.jsx
│   │   ├── ChartsSection.jsx (Chart.js Power & Diagnostics)
│   │   ├── LogsView.jsx (Real-time Derived Event Stream)
│   │   └── Footer.jsx
│   └── src/index.css (Glassmorphic Dark Theme)
```

---

## 1. Prerequisites & Installation

### Backend Dependencies (Python 3.13)
Install Flask, Flask-CORS, and NumPy:

```bash
cd C:\PolarSync
py -3.13 -m pip install -r requirements.txt
```

*(Or individually via `py -3.13 -m pip install flask flask-cors numpy`)*

### Frontend Dependencies (Node.js & npm)
Install React, Vite, and Chart.js dependencies:

```bash
cd C:\PolarSync\frontend
npm install
```

---

## 2. Running the System

### Terminal 1: Start the Python Simulator & Flask API
```bash
cd C:\PolarSync
py -3.13 data_simulator.py --demo
```

- Spawns the telemetry simulator in a background daemon thread.
- Streams live JSON records to stdout for CLI monitoring and debugging.
- Exposes the Flask REST API server on **`http://localhost:5000`**.
- Configurable interval:
  - Default: 60 seconds (intended simulation interval).
  - For hackathon demonstration: pass `--demo` or set `$env:INTERVAL_SECONDS="2"` for 2-second updates.

### Terminal 2: Start the React Frontend (Vite)
```bash
cd C:\PolarSync\frontend
npm run dev
```

Open your browser and navigate to:
**`http://localhost:3000`**

To produce an optimized production bundle:
```bash
npm run build
```

---

## 3. API Endpoint Documentation

### `GET /api/health`
Health check endpoint to verify that the Flask server is running.
- **URL**: `http://localhost:5000/api/health`
- **Method**: `GET`
- **Response**:
  ```json
  {
    "status": "ok"
  }
  ```

### `GET /api/data`
Returns the most recent station telemetry snapshot generated by the background simulation worker.
- **URL**: `http://localhost:5000/api/data`
- **Method**: `GET`
- **Response Schema**:
  ```json
  {
    "timestamp": "2026-09-07 11:49:48",
    "energy": 500.71,
    "generator_load": 59.38,
    "power_generation": 67.7,
    "power_consumption": 119.44,
    "fuel_level": 74.7,
    "battery_soc": 88.85,
    "heating": 15.2,
    "generator_health": 94.97,
    "pump_status": 1,
    "equipment_temperature": 34.99,
    "vibration": 1.93,
    "runtime": 1204,
    "food_stock_kg": 1840,
    "food_days_remaining": 92,
    "food_consumption_daily_kg": 20.0,
    "food_storage_temperature": -18.0,
    "food_status": "NORMAL",
    "food_expiry_risk": 2,
    "medicine_stock_units": 428,
    "medicine_days_remaining": 122,
    "medicine_consumption_daily": 3.5,
    "critical_medicine_items": 3,
    "low_medicine_items": 5,
    "medicine_expiry_risk": 2,
    "medicine_storage_temperature": 4.0,
    "medicine_status": "CRITICAL"
  }
  ```

---

## 4. Telemetry Field Mapping

| Field | Description | Display / Treatment |
|---|---|---|
| `energy` | Station grid reserve | Value (kWh) & capacity progress bar |
| `generator_load` | Turbine load percentage | Value (%) & load warning indicator |
| `power_generation` | Current generation rate | Value (kW) & plotted on Power Chart |
| `power_consumption` | Current station load | Value (kW) & plotted on Power Chart |
| `fuel_level` | Sub-surface fuel bunker level | Value (%) & level bar |
| `battery_soc` | Li-FePO4 State of Charge | Value (%) & reserve status |
| `heating` | Habitat climate thermal output | Value (kW) & heat output bar |
| `generator_health` | Generator operational health | Value (%) in summary bar |
| `pump_status` | Primary cooling pump state | `1` = Operational (green), `0` = Offline (red alert) |
| `equipment_temperature`| Core equipment temperature | Value (°C) & plotted on Diagnostics Chart |
| `vibration` | Piezo vibration sensor reading | Value (mm/s) & plotted on Diagnostics Chart |
| `runtime` | Generator runtime counter | Total elapsed hours/cycles |
| `timestamp` | Simulation timestamp | Displayed in "Last Updated" |
| `food_stock_kg` | Food supply inventory | Value (kg) & Canteen status |
| `medicine_stock_units`| Medicine inventory | Value (units) & Medical Center status |

---

## 5. End-to-End Data Flow

1. **Continuous Simulation Worker**:
   - `data_simulator.py` launches `run_simulator_background()` in a Python `threading.Thread(daemon=True)`.
   - Every `INTERVAL_SECONDS`, it calls `generate_data()`, which evaluates physical formulas, probability of anomalies, and clamping.
   - The thread prints the raw JSON record to stdout (preserving terminal streaming).
   - The thread safely locks and updates the shared global `latest_data` dictionary.

2. **Flask REST Service**:
   - The Flask server runs on port 5000 with Cross-Origin Resource Sharing (`CORS(app)`) enabled.
   - Incoming `GET /api/data` requests immediately read and return a copy of `latest_data` without blocking the simulator.

3. **Frontend Polling & Rendering**:
   - The React application (`src/App.jsx`) mounts into `#root` via Vite.
   - It polls `GET /api/data` every 2000ms using `fetchStationData()` in `src/services/api.js`.
   - On success: updates React state, sets status badge to **`● Simulator Connected`**, appends telemetry to history state for Chart.js, evaluates anomaly thresholds, and derives real-time event logs.
   - On error: switches status badge to **`● Simulator Disconnected`** and retains the last-known UI state gracefully.

---

## 6. Troubleshooting & FAQs

### Q: The frontend shows "● Simulator Disconnected"
- **Check if Flask is running**: Open `http://localhost:5000/api/health` in your browser. If you get `{"status": "ok"}`, the server is up.
- If not running, start it:
  ```bash
  py -3.13 C:\hackathon\data_simulator.py --demo
  ```
- **Port Conflict**: Check if another process is holding port 5000 (`Get-NetTCPConnection -LocalPort 5000`).

### Q: What about CORS errors?
- Flask-CORS is configured globally (`CORS(app)`).
- This allows cross-origin requests from the Vite frontend (`http://localhost:3000`).

### Q: Why do the charts cap at 30 points?
- To ensure optimal client-side performance and avoid memory leaks, the React `history` buffer keeps the latest 30 telemetry points.

### Q: How can I adjust the simulation speed?
- Default simulation interval is 60 seconds.
- For hackathon demonstrations, pass `--demo` or set `INTERVAL_SECONDS=2` to stream updates every 2 seconds.
- You can also set `SIMULATION_TIME_MULTIPLIER` (e.g. `SIMULATION_TIME_MULTIPLIER=60`) to accelerate simulated inventory consumption independently of the real-time polling interval.
